syntax = "proto3";

package login;

message RpcReq {
  bytes name = 1;
  bytes password = 2;
  int32 days = 3;
}

message RpcRes {
  string token = 1;
}

service AccountLogin {
  rpc Do(RpcReq) returns (RpcRes) {};
}
